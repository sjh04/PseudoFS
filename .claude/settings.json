{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -qE '\\.(cpp|h|hpp|cc)$' && clang-format -i \"$f\"; } 2>/dev/null || true",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
