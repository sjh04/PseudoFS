# 第 6 天进度报告

---

## 一、实验组整体进度

- **今日任务目标：** 完成 Shell 易用性附加项（通配符 glob 展开、Tab 命令补全、命令历史、per-command help）；TUI 磁盘块全屏热力图（F3）；系统性测试（单元测试 + 40+ e2e 黑盒场景）；`ll` 详细列与 UNIX 符号链接显示。
- **已完成模块：**
  - 通配符：glob 展开（`ls *.txt`、`rm *.log`），按当前目录匹配模式
  - 易用性：Tab 命令名补全 + 命令历史（↑↓ 翻阅）+ 每命令 `help` / `-h` 用法
  - TUI：F3 全屏磁盘块热力图（逐块着色显示空闲/数据/inode 区）
  - 测试：单元测试覆盖核心模块 + 40+ 条 e2e 黑盒验收场景（命令正常路径 + 边界条件）
  - `ll`：详细列（权限/大小/时间/owner）+ UNIX 符号链接 `name -> target` 显示
- **进行中模块：**
  - 联调发现的若干 bug（参数解析崩溃、TUI 渲染/刷新、弹窗残留）集中清理
  - README/报告同步、验收演示脚本
- **遇到的问题：**
  - glob 展开与带引号路径冲突（`"a b.txt"` 不应被当通配符）
  - Tab 补全候选排序与多候选展示
  - e2e 黑盒测试需脚本化喂命令并比对输出，ncurses 下不便自动化（改用 CLI 模式跑）
  - 热力图 512+ 块在有限面板宽度下的缩放映射

---

## 二、个人进度

### 组员一

- **姓名：** ______
- **整体任务：** 文件系统核心引擎
- **今日计划完成工作：** 为 `ll`/`stat` 提供完整 inode 元数据 + 配合热力图读块占用
- **实际完成工作：** 暴露 inode 完整字段（mode/nlink/uid/size/三时间戳）供 `ll`/`stat` 格式化；提供"块是否占用"查询供 F3 热力图逐块着色；补符号链接目标路径读取接口
- **遇到的困难及解决过程：** 热力图需遍历 512 个数据块的占用状态，而成组链是栈式管理、并非位图——临时维护一份占用快照（bitmap 视图）供可视化读取，不改动成组链本身；`ll` 的时间戳格式化（`uint32` epoch → 可读时间）按本地时间转换

### 组员二

- **姓名：** ______
- **整体任务：** 用户接口与系统控制
- **今日计划完成工作：** 通配符 glob + Tab 补全 + 命令历史 + help
- **实际完成工作：** glob 展开当前目录匹配 `*`/`?`；Tab 补全命令名（后续可扩展文件名）；命令历史环形缓冲 + ↑↓ 翻阅；每命令注册 usage 字符串，`help` / `-h` 打印用法
- **遇到的困难及解决过程：** glob 与引号冲突——先按引号切分保护"带空格/带 `*` 的字面量"，再对未引用 token 做 glob 展开；补全多候选时列出公共前缀并展示候选列表（无候选则响铃）

### 组员三

- **姓名：** ______
- **整体任务：** 用户管理与访问控制
- **今日计划完成工作：** 权限相关命令边界测试 + 配合 `ll` 显示 owner
- **实际完成工作：** 为 `login`/`useradd`/`passwd`/`chmod`/权限拒绝路径补 e2e 场景（非 root `useradd` 应拒绝、错密码登录失败、无 w 权限写失败）；`ll` 的 owner 列由 `UserManager` 反查用户名
- **遇到的困难及解决过程：** 权限拒绝与文件不存在两类错误此前都返回 −1，`ll`/`open` 无法区分——与组员二约定细化错误码（EACCES vs ENOENT），约定 Day 7 在 `open` 报错处落地区分提示

### 组员四

- **姓名：** ______
- **整体任务：** 测试文档与界面设计
- **今日计划完成工作：** F3 磁盘热力图 + 单元/e2e 测试体系 + 测试报告
- **实际完成工作：** F3 全屏逐块热力图（空闲/数据/inode 区分色，512+ 块按面板宽度缩放）；搭 GoogleTest 单元测试 + 40+ 条 e2e 黑盒脚本（CLI 模式喂命令、比对预期输出）；整理测试结果清单
- **遇到的困难及解决过程：** ncurses TUI 难以自动化测试——改走 CLI 模式（stdin 喂命令、stdout 比对）做 e2e，TUI 仅人工冒烟；热力图块数远超面板列数，按"每个字符代表 N 块、取该段最高占用色"压缩映射
