# 第 7 天进度报告

---

## 一、实验组整体进度

- **今日任务目标：** 集中清理联调暴露的 bug（参数解析崩溃、TUI 渲染/刷新/弹窗、双引擎数据串扰、错误提示）；全量 clang-format；同步 README；整理报告 4 个必答问题；验收演示脚本彩排。
- **已完成模块：**
  - 健壮性：修复非法/缺失命令参数导致 TUI 与 CLI 崩溃；`open` 失败区分 "permission denied" 与 "no such file"
  - TUI：启动即渲染面板 + 清理终端杂输出；每引擎独立 `BlockDevice` + 面板实时刷新（修 F2 切换数据串扰）；F1 帮助弹窗残留 "OP"/花屏修复；终端输入位置可见光标；ACS 线框 + 占用条 + 时钟 + 彩色提示符美化
  - 代码质量：全量 clang-format（Google 风格）
  - 文档：README 同步命令清单 / `--tui` / TUI 功能键 / root 登录说明；整理报告 4 个必答问题（卷结构 / 数据结构关系 / 格式化所作工作 / 影响文件长度的因素）
  - 验收：演示脚本（login → 建目录 → 建文件 → 写读 → 权限演示 → F2 切换 → 重复）彩排两轮
- **进行中模块：**
  - 验收彩排最后一轮 + 报告排版定稿
- **遇到的问题：**
  - 弹窗关闭后 `stdscr` 残留，需 `touchwin`/`redrawwin` 强制重绘
  - 多引擎切换刷新时序（切换后须先 mount/读再绘）
  - 演示用的权限演示账户（alice/bob 不同权限）准备

---

## 二、个人进度

### 组员一

- **姓名：** ______
- **整体任务：** 文件系统核心引擎
- **今日计划完成工作：** 引擎层稳定性收尾 + 配合双引擎独立 `BlockDevice`
- **实际完成工作：** 拆分两引擎各自独立的 `BlockDevice` 实例与镜像缓冲（修 F2 串扰根因）；复查 auto-sync 落盘完整性；边界用例（空文件、满盘、删根、跨二次间址大文件）回归
- **遇到的困难及解决过程：** 双引擎独立 `BlockDevice` 后，F2 切换须各自 mount/加载自己的镜像，确认两份磁盘数据互不污染；满盘时 `balloc` 返回失败的传播路径补齐（写到一半盘满时回滚已分配块，避免写半截留下脏 inode）

### 组员二

- **姓名：** ______
- **整体任务：** 用户接口与系统控制
- **今日计划完成工作：** 命令参数健壮性 + 错误提示细化
- **实际完成工作：** 所有命令对缺参/多参/非法参数做校验，避免越界崩溃；`open` 报错按错误码区分权限拒绝与文件不存在；统一错误信息文案
- **遇到的困难及解决过程：** 之前直接取 `args[1]` 不判长度导致缺参崩溃——统一在分发前校验参数个数并打印 usage；错误码 EACCES/ENOENT 从 VFS 一路透传到命令层，按码打印对应提示

### 组员三

- **姓名：** ______
- **整体任务：** 用户管理与访问控制
- **今日计划完成工作：** 权限演示数据准备 + 报告权限/用户管理部分
- **实际完成工作：** 准备演示账户（root/alice/bob，不同 owner/权限的文件）用于验收权限演示；撰写报告"访问控制模型（rwx + owner/group/other）"与"用户管理 / 持久化"小节
- **遇到的困难及解决过程：** 演示需体现"alice 不能写 bob 的文件、`chmod` 后可写"，据此设计目录/文件权限矩阵；持久化用户表与 `/home` 目录在 `format` 后的重建顺序——确认先恢复用户表、再校验 home 目录存在

### 组员四

- **姓名：** ______
- **整体任务：** 测试文档与界面设计
- **今日计划完成工作：** TUI 残留 bug 收尾 + 验收脚本 + 报告界面/测试部分
- **实际完成工作：** 修 F1 弹窗关闭后残留（`touchwin`+`redrawwin` 重绘父窗）、启动渲染、可见光标、ACS 美化；编写验收演示脚本并彩排；撰写报告"TUI 设计 + 测试方案 + 4 个必答问题"草稿
- **遇到的困难及解决过程：** 弹窗（`panel`）关闭后底层窗口不自动重绘，加 `update_panels()`+`doupdate()` 并对受遮挡窗 `touchwin`；F2 切换后面板时钟/占用条需立即刷新，统一在切换回调里触发全面板重绘
