# 第 5 天进度报告

---

## 一、实验组整体进度

- **今日任务目标：** 打通用户管理命令全套（`login`/`logout`/`useradd`/`passwd`/`su`），权限层改由登录用户驱动，用户表持久化跨重启保留；落地一批文件附加命令（`cp`/`ln`/`chmod`/`mv`/`tree`/`find`/`mkdir -p`/`cat`/`more`/`stat`）。
- **已完成模块：**
  - 用户命令：`login`/`logout`/`useradd`/`passwd`/`su` 全部打通，与 `UserManager` 联调
  - 权限驱动：FS 权限层改由"当前登录用户"驱动（替换占位 uid）
  - 持久化：用户表序列化落盘，重启后用户/密码保留
  - 文件命令：`cp` 复制；`ln` 硬链接（`nlink++`）与软链接（符号链接 inode）；`chmod`；`mv` 移动/重命名；`tree`（`-d N` 限深）；`find` 按名搜索；`mkdir -p` 多级建目录；`cat`/`more` 查看；`stat` 显示 inode 详情
- **进行中模块：**
  - 通配符 glob 展开、Tab 补全/命令历史/help
  - 磁盘块全屏可视化、e2e 测试
  - `ll` 详细列样式
- **遇到的问题：**
  - 软链接与硬链接语义差异（软链接存路径字符串、可悬空；硬链接共享 inode、`nlink` 计数）
  - `su` 与 `login` 的栈式切换（`su` 后 `exit` 返回原用户）
  - `mv` 跨目录移动需同时改源/目的父目录的目录项
  - 持久化用户表与 `/etc/passwd` 模拟文件的一致性

---

## 二、个人进度

### 组员一

- **姓名：** ______
- **整体任务：** 文件系统核心引擎
- **今日计划完成工作：** 配合 `cp`/`ln` 的底层 inode 支持 + 符号链接 inode 类型
- **实际完成工作：** 为 `ln` 提供硬链接（目录项指向同一 inode、`nlink++`）与软链接（新建 symlink 类型 inode、数据块存目标路径）底层支持；`cp` 的"读源 inode 数据块 → 分配新 inode → 逐块复制"路径；`namei` 增加遇符号链接时解引用（限定跳数防环）
- **遇到的困难及解决过程：** 硬链接删除时只在 `nlink` 归 0 才真正 `ifree`+`bfree`，否则只删目录项——在 `iput` 中按 `nlink` 判定是否回收；符号链接解引用要防自指环，加最大跳数（8 次）上限，超限报"too many levels of symbolic links"

### 组员二

- **姓名：** ______
- **整体任务：** 用户接口与系统控制
- **今日计划完成工作：** `cp`/`ln`/`chmod`/`mv`/`tree`/`find`/`mkdir -p`/`cat`/`more`/`stat` 命令落地
- **实际完成工作：** 上述命令全部实现并接入分发；`tree` 支持 `-d N` 限深递归；`find` 全树按名匹配；`mkdir -p` 逐级创建、已存在则跳过；`more`/`cat` 分页/全量查看；`stat` 打印 inode/大小/时间/权限/链接数
- **遇到的困难及解决过程：** `mv` 跨目录移动——分解为"在目的目录加目录项 + 从源目录删目录项"，同目录则只改名；`tree` 递归深度与缩进对齐，用前缀字符串逐层拼接（`├──`/`└──`/`│`）画树枝

### 组员三

- **姓名：** ______
- **整体任务：** 用户管理与访问控制
- **今日计划完成工作：** `login`/`logout`/`useradd`/`passwd`/`su` + 权限层由登录用户驱动 + 用户表持久化
- **实际完成工作：** `login` 校验密码并设当前用户、`logout` 回登录态；`useradd`（root 限定）建用户 + `/home` 目录；`passwd` 改密；`su` 栈式切换用户；FS 权限检查改读 `UserManager` 当前 uid/gid；用户表序列化落盘、重启恢复
- **遇到的困难及解决过程：** `su` 嵌套切换需用栈保存前一用户，`exit` 弹栈恢复；权限层从占位 uid 切到真实登录用户后，root 用 `useradd` 建的 `/home/<user>` owner 须归属新建用户（`useradd` 后把 home 目录 chown 到该用户）

### 组员四

- **姓名：** ______
- **整体任务：** 测试文档与界面设计
- **今日计划完成工作：** 测试用例补充 + 目录树/磁盘面板配合新命令刷新
- **实际完成工作：** 为 `cp`/`ln`/`mv`/`tree`/`find` 补单元测试；TUI 目录树在命令执行后刷新反映 `mkdir`/`rm`/`mv` 结果；磁盘面板随分配/回收实时更新占用条
- **遇到的困难及解决过程：** 软链接在目录树中显示为 `name -> target` 需读 symlink 数据块，与组员一约定了读取接口；F5 手动刷新 vs 命令后自动刷新——先在写类命令执行后触发刷新，保证界面与磁盘状态一致
