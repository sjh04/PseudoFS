# 第 2 天进度报告

---

## 一、实验组整体进度

- **今日任务目标：** 完成项目架构搭建，各模块进入编码阶段。块设备层基本读写能力就绪，VFS 接口定义完成，Shell 命令框架跑通，用户管理数据结构设计完成，TUI 布局方案定稿。
- **已完成模块：**
  - 项目构建系统（CMake + 目录结构）
  - VFS 抽象接口 `IFileSystem` 定义
  - 块设备层 `BlockDevice` 基本读写
  - 超级块数据结构设计与初始化逻辑框架
  - Shell 命令注册与分发框架
  - 用户管理数据结构设计
- **进行中模块：**
  - 块设备层：格式化逻辑尚未完成
  - UNIX inode 管理器：空闲 inode 分配/回收仍在编码
  - FAT16 引导扇区：结构定义完成，格式化未开始
  - Shell：仅跑通框架，具体命令未实现
  - TUI：仅完成布局方案草图，尚未编码
- **遇到的问题：**
  - 混合索引寻址逻辑较复杂，组员一在 `bmap()` 间接块转换上需要进一步梳理
  - ncurses 宽字符库链接问题（`libncursesw5-dev` 未安装导致编译报错，已解决）
  - 成组块链接法中空闲块栈的初始化和跨组链接需要再确认细节

---

## 二、个人进度

### 组员一

- **姓名：** ______
- **整体任务：** 文件系统核心引擎
- **今日计划完成工作：** BlockDevice 读写接口实现 + SuperBlock 初始化 + 空闲 inode 栈 & 空闲块栈数据结构编码
- **实际完成工作：** BlockDevice 基本读写、seek 接口完成并通过简单自测；SuperBlock 结构体定义并写入磁盘第 0 块；空闲 inode 栈初始化逻辑完成（`ialloc` / `ifree` 函数框架）；空闲块成组链数据结构编码中
- **遇到的困难及解决过程：** `bmap()` 中单间接块与双间接块的地址转换容易出错——用纸笔推演了一遍块号计算过程后理清；空闲块栈的跨组链接（下一组首块号存入当前组最后一个位置）逻辑绕了一下，对照 PPT 示意图画出链表结构后解决

### 组员二

- **姓名：** ______
- **整体任务：** 用户接口与系统控制
- **今日计划完成工作：** Shell 命令解析框架 + 命令注册表 + 部分基础命令（format、login、logout、pwd、cd）
- **实际完成工作：** Shell 层 `CommandRegistry` 注册/分发框架完成，`tokenize` 分词器支持引号字符串；`login`、`logout`、`pwd` 命令骨架完成并通过 VFS 空接口联调；`cd` 路径解析（`.` / `..` / `~`）逻辑编写中
- **遇到的困难及解决过程：** ncurses 编译时链接 `-lncurses` 报未定义引用，原因是需要宽字符版本 `-lncursesw`，安装 `libncursesw5-dev` 后解决；分词器对带空格的引号路径处理最初遗漏，补了一版正则匹配后通过

### 组员三

- **姓名：** ______
- **整体任务：** 用户管理与访问控制
- **今日计划完成工作：** 用户数据结构（User、UserManager）设计 + 用户文件持久化方案 + `useradd` / `passwd` 逻辑设计
- **实际完成工作：** `User` 结构体定义完成（uid、username、password hash、home 目录）；`UserManager` 类框架搭建，支持 add / auth / getById 接口；用户数据持久化方案确定（存入 `/etc/passwd` 模拟文件）；`useradd` 自动创建 `/home/<username>` 目录的逻辑设计完成
- **遇到的困难及解决过程：** 密码存储方案讨论——明文 vs 简单 hash，考虑到课设性质决定采用简单异或混淆而非真 hash（后续可升级）；用户 ID 分配策略：确定为自增 ID，最大 8 用户，删除用户后 ID 不复用

### 组员四

- **姓名：** ______
- **整体任务：** 测试文档与界面设计
- **今日计划完成工作：** TUI 布局方案定稿 + 测试用例清单编写 + 验收文档模板
- **实际完成工作：** TUI 四面板布局（目录树 + 磁盘状态 + 终端 + 功能键栏）方案定稿，已在纸上画出 ASCII 布局图；测试用例清单初版完成（涵盖 20 条命令 × 正常路径 + 边界条件）；验收流程脚本大纲完成；开始调研 ncurses `pad` / `window` / `panel` API 用法
- **遇到的困难及解决过程：** ncurses 的 panel 库（`libpanel`）需要额外链接，在 CMakeLists.txt 已补充；目录树实时刷新 vs 手动刷新的取舍——决定先做 F5 手动刷新，后续若性能允许再改为自动
